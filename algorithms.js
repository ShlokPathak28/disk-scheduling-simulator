const plannedAlgorithms = ["FCFS", "SSTF", "SCAN", "C-SCAN"];
const projectStage = "day-1-scaffold";

if (typeof window !== "undefined") {
    window.plannedAlgorithms = plannedAlgorithms;
    window.projectStage = projectStage;
}
